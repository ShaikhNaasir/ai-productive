# Production build config for the React client.
# Vite automatically loads this file when running `vite build` (production mode).
# Point it at your deployed API origin.
VITE_API_URL="https://your-api.onrender.com"
